# 方法说明

## 状态是第一层输出

苏牙择时每天计算一次，通常在约 20:00 形成下一交日的状态。它只预测下一交易日的大盘方向，适用于宽基市场，不是只预测中证500。当前来源是 `signal_db.public.jq_time_series_signal_daily` 中策略 `ret_trend_lev_ma_5level_calendar` 版本 `3.2`；`prediction` 只允许 -2、-1、0、1、2 五档，并分别映射为强空、弱空、看平、弱多、强多。

账本把表中的 `date` 记录为 `as_of_trade_date`，把预测周期明确写成 `next_trading_session`。这避免把来源观察日期误写成预测生效日期。信号属于短周期判断，常见持续时间约为两到三个交易日，也可能每天变化。

五档状态描述方向和强弱。看平不是中性区间，而是比弱多更弱一档的看多，对应很轻微的多头暴露。五档状态不是个性化买卖建议，也不是固定仓位指令。

## 风险暴露是第二层映射

风险暴露回答“在某个市场状态下使用多少仓位”。这套信号整体右偏，以多头暴露为主要收益来源。弱空主要表示降低多头仓位、规避风险；只有强空时，能承担做空风险的人才可考虑极轻微空头。相同状态下，保守、平衡和积极的风险偏好应有不同映射；公开账本不假设所有人采用同一仓位。

后续工具可以提供三套预设模板；自定义映射需要单独进行样本内、样本外和压力测试，不能把历史回撤描述为未来保证。

## 子策略是后续扩展

小微盘、科技等风格策略是否开启，属于市场状态之上的子策略开关。它们不进入当前 MVP，避免把择时状态、仓位映射和风格选择混成一个无法验证的输出。

## 证据标准

1. **只读来源**：Publisher 在 `BEGIN READ ONLY` 事务中读取最新一行，并验证日期、level 与 label。
2. **事前提交**：状态发生前公开 SHA-256 commitment。
3. **标准化**：使用 RFC 8785 canonical JSON，避免字段顺序或语言实现造成歧义。
4. **隐藏状态**：加入 32 字节随机 nonce，防止在只有五种状态时被穷举。
5. **外部见证**：公开文件由 Sigstore 签名并进入 Rekor 透明日志；JSON 中自报的时间不是唯一依据。
6. **延迟揭示**：五个后续交易观测日后公开状态、时间和 nonce。
7. **完整记录**：成功、失败和误导阶段采用相同规则，不挑选展示。
8. **只追加修订**：原始文件不覆盖，错误通过修订日志解释。
9. **责任统一**：公开结论以宋伟力本人为表达与解释主体。

## 为什么从上线日开始

`jq_time_series_signal_daily` 的历史行与 `created_at` 本身不能证明某个过去日期当时已经形成了预测。因此历史行可以用于回溯研究，但不能回填成事前 commitment。历史聚合评价从该表读取信号，并按日期连接行情收益；原始逐日序列不公开。

## 数据源迁移

从 2026-08-07 起，新承诺固定到策略 3.2、参数哈希 `98bc3197708958de` 和 `IC.CFE`。迁移前的承诺保留 `aistk.public.micro_timing_final_tail_hold_dates:trend5mcx` 原来源标识；验证器同时识别两种已冻结来源，并要求 commitment 与 reveal 的来源完全一致。迁移不修改既有文件或哈希链。

首条旧来源记录因 `trade_date` 语义错位，已通过只追加修订日志标记为无效，不作为事前预测，也不计入前瞻绩效；原 commitment 与哈希证据仍完整保留。

公开账本只认可自动化上线之后、在外部透明日志留下时间见证的记录。

## 评价规则已经冻结

信号效果不由哈希本身证明。当前有效口径为 [《择时信号评价方法 v1.1》](./EVALUATION_V1_1.md)：每条信号只评价下一交易日的宽基大盘方向；看平按最弱看多处理，不使用波动率中性带宽；连续状态区间只作描述性研究，不作为信号评分。v1.0 的错误口径保留在[修订记录](./CORRECTIONS.md)中。

## 公开窗口与历史隔离

页面和仓库当前视图最多公开最近 20 个交易日的信号窗口。窗口外原始信号仍在仓库外私有保存，只将全量数据的聚合评价和 SHA-256 快照哈希写入公开仓库。回溯样本与上线后事前 commitment 样本分开标记和统计。

这个设计只用于防止新访客从当前页面或仓库一次性取得完整序列，不用于对抗持续归档。已公开的 reveal 可能已被第三方保存，Git 历史也仍可追溯。完整边界见 [发布规则 v1](./PUBLICATION_POLICY_V1.md)。
