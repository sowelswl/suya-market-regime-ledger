# 公开窗口与历史评价发布规则 v1

状态：**已冻结**

版本：`1.0.0`

生效日期：`2026-08-07`

本规则解决两个同时存在的目标：普通读者可以核对近期真实信号和全部历史评价，但不能从仓库当前视图一次性下载完整逐日信号序列。机器可读规则见 [`publication/v1.json`](./publication/v1.json)。

## 最近 20 个交易日窗口

- 页面和仓库当前分支只展示最近 20 个 commitment 日期对应的信号窗口。
- 窗口外 commitment 继续保留，用于证明当时存在过一条隐藏状态的承诺。
- 窗口外 reveal 从当前分支删除；完整原始信号继续保存在仓库外的本地私有档案中。
- 每次每日发布完成后自动滚动窗口，不依赖人工清理。

这是一项面向普通访问者的轻度保护。已经公开过的文件仍可能被第三方保存，Git 历史也仍可追溯；本规则不声称能够阻止持续爬取、镜像或专业取证。

## 全部历史只发布聚合评价

历史数据在只读事务中按 [评价方法 v1.1](./EVALUATION_V1_1.md) 计算，只使用下一交易日收益。公开报告包括：

- 完整样本范围与可评价数量；
- 中证500和沪深300的总体方向命中率、平均及中位下一日收益；
- 五档状态的样本数、方向命中率和聚合收益；
- 样本足够年份的年度聚合；
- 不包含原始行的数据快照 SHA-256。

公开报告不包含逐日日期—状态序列、逐日仓位或逐日策略净值。单档少于 10 条、单年少于 20 条时只报告样本不足，不展示详细指标。

历史评价属于回溯研究，不具备上线后 commitment 的事前时间证明，也不与事前账本样本混算。
