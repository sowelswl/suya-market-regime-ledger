# 择时信号评价方法 v1

状态：**已冻结**

版本：`1.0.0`

生效日期：`2026-08-07`

本文件在看到足够结果之前固定评价口径。对应的机器可读规则是 [`evaluation/v1.json`](./evaluation/v1.json)。v1 不得原地改写；如果方法需要改变，只能发布带未来生效日期的新版本，历史结果继续按当时有效的版本计算。

## 1. 评价对象

v1 的唯一主评价标的是**中证500指数（000905）官方收盘点位**。它与个人实盘报告中的公开基准一致，也避免在看到结果后从多个指数或市场宽度指标中挑选表现最好的一个。

- `t` 是承诺中的 `as_of_trade_date`；信号约在当日收盘后形成。
- `close[t]` 是中证500在 `t` 的官方收盘点位。
- `h` 只按该指数存在官方观测的交易日推进，不用自然日补值。
- v1 使用价格收益，不混入股息、仓位、交易成本或个人持仓。

如果官方点位缺失，该条对应期限记为“不可评价”，不得使用前值、插值或其他指数替代。

## 2. 每日评价

每一条有效且已经揭示的 commitment 都是一条每日预测；连续多日状态不变时仍逐日保留，不能只挑状态切换日。

观察期限为未来 1、2、3 个交易日，其中**下一交易日是主口径**：

```text
R(t, h) = close[t + h] / close[t] - 1
h ∈ {1, 2, 3}
```

### 方向与看平

- 弱多、强多：`R(t,h) > 0` 为方向命中，等于零或小于零为未命中。
- 弱空、强空：`R(t,h) < 0` 为方向命中，等于零或大于零为未命中。
- 看平：使用事前可得的动态波动区间，而不是事后挑选固定阈值。

看平带宽为：

```text
B(t,h) = 0.5 × σ20(t) × sqrt(h)
```

其中 `σ20(t)` 是截至 `t`、包含当日收益在内的最近 20 个中证500日收益的样本标准差。`|R(t,h)| ≤ B(t,h)` 记为看平命中；不足 20 个历史收益时不评分。

### 强弱程度

强多与弱多、强空与弱空在“方向命中率”里采用相同方向规则。强弱程度不另造一个事后阈值，而是通过以下分布指标评价：

- 各档状态的样本数、平均收益、中位数和分位数；
- 强多相对弱多的后续收益差；
- 强空相对弱空的后续收益差；
- 五档 level 与后续收益的等级相关和单调性。

因此 v1 不把五档状态压缩成一个容易被误读的“五分类准确率”。

## 3. 完整状态区间评价

连续、有效、已揭示且 `signal_level` 相同的记录合并成一个状态区间。

- 入场点：区间第一条记录 `as_of_trade_date` 的官方收盘点位。
- 出场点：区间最后一条同状态记录之后一个交易日的官方收盘点位。
- 最新尚未结束的区间标记为 open，不进入已完成区间统计。
- 缺失记录或无效修订会终止区间，不跨缺口拼接。

每个已完成区间统计：交易日长度、区间收益、方向命中、按每日收盘计算的最大有利变动和最大不利变动。

每日评价回答“每晚形成的判断是否具有统计关系”；完整状态区间评价回答“状态连续维持两三天时，实际持有阶段如何”。两套结果同时保留，互不替代。

## 4. 展示门槛

在达到门槛前，公开页面只展示运行天数、承诺数、揭示数、缺失与修订，不发布胜率结论。

- 少于 20 条已揭示记录：只报告证据链和运行完整度。
- 至少 20 条已揭示记录：可以发布标明“初步”的总体每日统计。
- 单档状态至少 10 条后，才展示该档指标。
- 至少 10 个已完成状态区间后，才展示区间汇总。

样本门槛只解决最基本的可读性，不代表统计显著，也不能转化为未来收益保证。

## 5. 缺失、修订与失败

- 原始 commitment 和 reveal 永不替换。
- 被只追加修订标记为无效的记录，不进入预测效果指标，但仍计入运行失败率。
- 数据中断、未按时发布和未按期揭示必须单列；不得补写为正常记录。
- 任何案例文章只用于解释当时思路，不改变评价样本与计算规则。

## 6. 暂不纳入 v1 的内容

风险暴露模板、子策略开关和个性化仓位组合收益属于下一层产品验证。它们可以在未来使用同一批事前状态做独立评价，但不得混入 v1 后再把组合收益称为“信号胜率”。

v1 先回答一个更窄的问题：五档市场状态与中证500未来 1–3 个交易日的方向、幅度及完整状态区间表现之间，是否存在稳定、可重复的关系。
